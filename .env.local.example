# Local Development Environment Configuration
# Copy this file to .env.local for local testing

# ====================================
# FRONTEND CONFIGURATION (OPTIONAL)
# ====================================
# The frontend automatically uses:
# - Development: http://localhost:8000 (direct connection)
# - Production: /api/ml-proxy (Vercel serverless proxy)
# You don't need to set VITE_ML_API_URL anymore

# Enable debug logging (optional)
VITE_DEBUG=true

# Frontend dev server port (optional)
VITE_PORT=5173

# ====================================
# VERCEL ENVIRONMENT VARIABLE
# ====================================
# Set this in Vercel Dashboard > Settings > Environment Variables:
# 
# Variable Name: VITE_ML_BACKEND_URL
# Variable Value: http://13.219.229.219:8000
# 
# This tells the /api/ml-proxy serverless function where your AWS ECS backend is running.
# Update the IP whenever your ECS service restarts (or use ALB for permanent URL).
